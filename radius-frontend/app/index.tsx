export default function Index() {
    // Auth routing is handled by LoadingLayout in _layout.tsx.
    // This component intentionally renders nothing to avoid competing navigations.
    return null;
}
